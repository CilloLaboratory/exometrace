process.executor = 'slurm'

process.withLabel: cpu_small {
  queue = 'general'
}

process.withLabel: cpu_medium {
  queue = 'general'
}

process.withLabel: cpu_large {
  queue = 'general'
}

process.withLabel: gpu {
  queue = 'gpu'
  clusterOptions = '--gres=gpu:1'
}
